document.addEventListener('DOMContentLoaded', function() {
    console.log("AI Page yuklandi va tayyor!");

    // Kelgusida tugmalar bosilganda yoki interaktiv vizuallashtirish kerak bo'lsa
    const actionBtn = document.querySelector('.btn-primary-action');

    if (actionBtn) {
        actionBtn.addEventListener('click', function(e) {
            // Tugma bosilganda o'tish effekti
        });
    }
});